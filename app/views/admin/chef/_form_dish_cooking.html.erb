<% cooking_dishes.each do |dish| %>
  <li class="list-group-item list-group-item-info" type="order_dishes"
    order-id="<%= dish.order_id %>" id="<%= dish.id %>">
    <%= render "data", food: dish %>
    <%= render "time_counter", type: dish %>
  </li>
<% end %>
