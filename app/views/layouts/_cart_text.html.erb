<%= link_to cart_path, id: "order-card-text" do %>
  <i class="fa fa-shopping-cart"
    aria-hidden="true"></i>
  <span class="badge">
    <small>
      <%= current_order.total_item %>
    </small>
  </span>
<% end %>
